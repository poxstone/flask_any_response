apiVersion: v1
kind: Service
metadata:
  name: flask-any-service-a
spec:
  selector:
    app: flask-any-response-a
  ports:
    - protocol: TCP
      name: tcp
      port: 8080
      targetPort: 8080
    #- protocol: UDP
      name: udp
    #  port: 5005
    #  targetPort: 5005
  type: LoadBalancer
  #type: NodePort
  selector:
    app: flask-any-response-a
