apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: myapp-gateway
  namespace: default
spec:
  selector:
    istio: ingressgateway
  servers:
  - hosts:
    - "*"
    port:
      name: http
      number: 8080
      protocol: HTTP
    #tls:
    #  mode: SIMPLE
    #  credentialName: wildcard-credential
